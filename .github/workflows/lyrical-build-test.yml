name: Lyrical Build and Test

on:
  push:
  pull_request:
  schedule:
    - cron: "0 5 * * 1"

jobs:
  lyrical_build_and_test:
    runs-on: ubuntu-latest
    container:
      image: ros:lyrical
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build and Test
        uses: ros-tooling/action-ros-ci@v0.4
        with:
          target-ros2-distro: lyrical
          coverage-result: false
          skip-tests: true
          colcon-defaults: |
            {
              "test": {
                "parallel-workers" : 1
              }
            }
